[General]
#################### General ####################

ScreenWidth="2560"
ScreenHeight="1440"
ScreenPadding="0"
# Default 0, Options: from 0 to min(screen width/2,screen height/2).

Font="Eternal UI"
HeaderFont="Eternal UI 2"
# Optional: font for the header text and the clock (bold; the Eternal UI bold weights are caps-only with the slashed O).
FontSize="14"
# Default is screen height divided by 80 (1080/80=13.5), Options: 0-inf.

KeyboardSize="0.4"
#  Default 0.4, Options 0.1-1.0

RoundCorners="4"

Locale=""
# Locale for data and time format. I suggest leaving it blank.
HourFormat="HH:mm"
# Default Locale.ShortFormat.
DateFormat="dddd, d MMMM"
# Default Locale.LongFormat.

HeaderText="RIP AND TEAR"
# You can put something fun.

#################### Background ####################

BackgroundPlaceholder=""
# Must be a relative path.
# Background displayed before the actual background is loaded.
# Use only if the background is a video, otherwise leave blank.
# Connected with: Background.
Background="Backgrounds/doom.jpeg"
# Must be a relative path.
# Supports: png, jpg, jpeg, webp, gif, avi, mp4, mov, mkv, m4v, webm.
BackgroundSpeed=""
# Default 1.0. Options: 0.0-10.0 (can go higher).
# Speed of animated wallpaper.
# Connected with: Background.
PauseBackground=""
# Default false.
# If set to true, stops playback of gifs. Works only with gifs.
# Connected with: Background.
DimBackground="0.15"
# Options: 0.0-1.0.
# Connected with: DimBackgroundColor
CropBackground="true"
# Default false.
# Crop or fit background.
# Connected with: BackgroundHorizontalAlignment and BackgroundVerticalAlignment doesn't work when set to true.
BackgroundHorizontalAlignment="center"
# Default: center, Options: left, center, right.
# Horizontal position of the background picture.
# Connected with: CropBackground must be set to false.
BackgroundVerticalAlignment="center"
# Horizontal position of the background picture.
# Default: center, Options: bottom, center, top.
# Connected with: CropBackground must be set to false.

#################### Colors ####################
# DOOM Eternal menu palette: olive panels with a light-green outline, orange for the active item.

HeaderTextColor="#ff3b1f"
DateTextColor="#ffb347"
TimeTextColor="#ffe9c4"

FormBackgroundColor="#120404"
BackgroundColor="#120404"
DimBackgroundColor="#0a0000"

LoginFieldBackgroundColor="#2f3d16"
PasswordFieldBackgroundColor="#2f3d16"
LoginFieldTextColor="#dcf28a"
PasswordFieldTextColor="#dcf28a"
UserIconColor="#a9d43a"
PasswordIconColor="#a9d43a"

PlaceholderTextColor="#7f9a3c"
WarningColor="#ff3b1f"

LoginButtonTextColor="#1c0e00"
LoginButtonBackgroundColor="#f2861c"
SystemButtonsIconsColor="#a9d43a"
SessionButtonTextColor="#a9d43a"
VirtualKeyboardButtonTextColor="#a9d43a"

DropdownTextColor="#dcf28a"
DropdownSelectedBackgroundColor="#f2861c"
DropdownBackgroundColor="#2f3d16"

HighlightTextColor="#1c0e00"
HighlightBackgroundColor="#f2861c"
HighlightBorderColor="#e9ff7a"
FieldBorderColor="#a9d43a"
# Outline of the chamfered Eternal-style fields, buttons and dropdowns when not focused (HighlightBorderColor when focused).

HoverUserIconColor="#e9ff7a"
HoverPasswordIconColor="#e9ff7a"
HoverSystemButtonsIconsColor="#e9ff7a"
HoverSessionButtonTextColor="#e9ff7a"
HoverVirtualKeyboardButtonTextColor="#e9ff7a"

#################### Form ####################

PartialBlur="true"
# Default false.
FullBlur="false"
# Default false.
# If you use FullBlur I recommend setting BlurMax to 64 and Blur to 1.0.
BlurMax="24"
# Default 48, Options: 2-64 (can go higher because depends on Blur).
# Connected with: Blur.
Blur="1.5"
# Default 2.0, Options: 0.0-3.0 (without 3.0).
# Connected with: BlurMax.

HaveFormBackground="true"
# Form background is transparent if set to false.
# Connected with: PartialBlur and BackgroundColor.
FormPosition="left"
# Default: left, Options: left, center, right.

#################### Virtual Keyboard ####################

VirtualKeyboardPosition="center"
# Default: left, Options: left, center, right.

#################### Interface Behavior ####################

HideVirtualKeyboard="true"
HideSystemButtons="false"
HideLoginButton="false"

UseRealName="false"
# If set to true, the user's real name (GECOS) is displayed instead of the account username. Users can log in using either their username or real name. If set to false, users can log in only with their username.
# Warning: If two or more accounts have the same real name, they cannot be distinguished during login. In that case, it is recommended to set this option to `false`.

ForceLastUser="true"
# If set to true last successfully logged in user appears automatically in the username field.
PasswordFocus="true"
# Automatically focuses password field.
HideCompletePassword="true"
# Hides the password while typing.
AllowEmptyPassword="false"
# Enable login for users without a password.
BypassSystemButtonsChecks="false"
# Skips checking if sddm can perform shutdown, restart, suspend or hibernate, always displays all system buttons.
RightToLeftLayout="false"
# Revert the layout either because you would like the login to be on the right hand side or SDDM won't respect your language locale for some reason. This will reverse the current position of FormPosition if it is either left or right and in addition position some smaller elements on the right hand side of the form itself (also when FormPosition is set to center).

#################### Translation ####################

# These don't necessarily need to translate anything. You can enter whatever you want here.
TranslatePlaceholderUsername=""
TranslatePlaceholderPassword=""
TranslateLogin=""
TranslateLoginFailedWarning=""
TranslateCapslockWarning=""
TranslateSuspend=""
TranslateHibernate=""
TranslateReboot=""
TranslateShutdown=""
TranslateSessionSelection=""
TranslateVirtualKeyboardButtonOn=""
TranslateVirtualKeyboardButtonOff=""
